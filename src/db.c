#include <../include/ottomatta.h>

/*
Parses given affirm and variable structs.

The structs are given as a string via REPL or API.
A regular struct create command looks like:

'`"fieldname1 fieldtype1", "fieldname1 fieldtype1"`' 

*/

void parse_struct(char *buffer, int size) {
    
}